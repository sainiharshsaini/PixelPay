{
  "name": "bank-webhook",
  "version": "1.0.0",
  "description": "Webhook service for handling bank transactions securely",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx esbuild ./src/index.ts --bundle --platform=node --outfile=dist/index.js",
    "start": "node dist/index.js",
    "dev": "pnpm run build && pnpm run start"
  },
  "keywords": [],
  "author": "Harsh Saini",
  "license": "ISC",
  "dependencies": {
    "@repo/db": "workspace:*",
    "@repo/validation-schemas": "workspace:*",
    "dotenv": "^17.2.1",
    "esbuild": "^0.25.2",
    "express": "^5.1.0",
    "pino": "^9.8.0"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/express": "^5.0.1",
    "@types/node": "^24.1.0",
    "ts-node": "^10.9.2"
  }
}
